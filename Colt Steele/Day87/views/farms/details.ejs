<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Details <%= farm.name %></title>
  </head>
  <body>
    <h1>Details of the farm</h1>
    <ul>
      <li>Name: <%= farm.name %></li>
      <li>place - <%= farm.city %></li>
      <li>Email : <%= farm.email %></li>
    </ul>
    <h2>Products</h2>
    <ul>
      <% for(let product of farm.products){ %>
      <li><a href="/products/<%= product._id %>"><%= product.name %></a></li>
      <% } %>
    </ul>
    <a href="/farms">Back to All farms</a>
    <a href="/farms/<%= farm.id %>/products/add ">Add products</a>

    <form action="/farms/<%= farm._id %>?_method=DELETE " method="post">
      <button type="submit">Delete</button>
    </form>
  </body>
</html>
